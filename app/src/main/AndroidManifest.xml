<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moontechit.jwellerscalculator">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Sub_Activity"></activity>
        <activity android:name=".Web_View" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Sponsored"
            android:label="Sponsored By" />
        <activity
            android:name=".Sum_Acitivity"
            android:label="গহনার যোগ" />
        <activity
            android:name=".Tk_Activity"
            android:label="টাকার হিসাব" />
        <activity android:name=".Splash_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>